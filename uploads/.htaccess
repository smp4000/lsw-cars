# PHP-Ausführung im Upload-Ordner unterbinden (keine php_flag-Direktiven,
# damit auch Shared-Hoster ohne AllowOverride Options funktionieren).
<FilesMatch "\.(php|phtml|phar|cgi|pl|py|inc)$">
  Require all denied
</FilesMatch>
